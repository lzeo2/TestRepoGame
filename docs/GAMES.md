# Game Catalog — UNBLOCKMATH // ARCADE

All **42 registered games** from `games.json` (ids 1–60 with gaps — the ids of removed games are not reused). Category comes from the catalog `cat` field; controls and status are based on the actual files in `Games/`.

**Status key**

| Mark | Meaning |
| --- | --- |
| ✅ Offline | Fully self-contained; works with zero network access. |
| ⚠️ Offline-capable | Gameplay runs offline; cosmetic external references (fonts, favicons, links) or bundle attempts that fail silently. |
| ❌ Remote-dependent | Requires network to actually play (embedded iframe or runtime CDN/signaling). **Not offline.** |

---

| # | Title | Cat | Controls | Status |
| --- | --- | --- | --- | --- |
| 1 | 2048 | classic | W / A / S / D (arrow keys and touch are not bound in this build) | ⚠️ Offline-capable — external favicon (`i.ibb.co`) only |
| 2 | Age of War | strategy | Mouse (Flash game) | ❌ Remote-dependent — loads Ruffle from `unpkg.com` CDN at runtime |
| 3 | Slope | action | Arrow keys (embedded game) | ❌ Remote-dependent — full-page iframe to `AidanTangTPS.github.io/Slope-Game` |
| 4 | Soccer Random | sports | Arrows + Space (wrapper binds these; in-game C3 bindings per original) | ⚠️ Offline-capable — local Construct 3 export |
| 5 | Basket Random | sports | Arrows + Space (wrapper binds these; in-game C3 bindings per original) | ⚠️ Offline-capable — local Construct 3 export |
| 6 | Volley Random | sports | Arrows + Space (wrapper binds these; in-game C3 bindings per original) | ⚠️ Offline-capable — local Construct 3 export |
| 7 | Ovo | action | WASD / Arrows move · Space / Up jump · Shift run (standard Ovo) | ⚠️ Offline-capable — bundle attempts a tips fetch + adblock-check XHR that fail silently offline |
| 8 | Run 3 | action | Arrow keys — run/jump, Left/Right rotate the tunnel | ⚠️ Offline-capable — self-contained bundle; contains external ad/store link hooks (not gameplay fetches) |
| 9 | Snake | classic | WASD / Arrows · on-screen mobile buttons | ✅ Offline |
| 10 | Chrome Dino | classic | Space / Up jump · Down duck · tap to jump | ✅ Offline |
| 11 | Breakout | classic | Left/Right arrows · touch drag | ⚠️ Offline-capable — external Google Fonts link (cosmetic, falls back); dead `compiled.css` link |
| 12 | Hextris | classic | Arrow keys (embedded game) | ❌ Remote-dependent — full-page iframe to `hextris.github.io/hextris` |
| 13 | Flappy Bird | classic | Tap / click / Space (embedded game) | ❌ Remote-dependent — full-page iframe to `flappybird.io` |
| 14 | Character Alsen | riddle | Type a message, press send/Enter | ✅ Offline — **scripted** local keyword-matching chatbot (not a hosted LLM). Folder is READ-ONLY. |
| 15 | QWOP | sports | Q = left thigh · W = right thigh · O = left calf · P = right calf | ✅ Offline (registered entry points at `Games/QwopRemake/`) |
| 16 | Star Catcher | action | Left/Right arrows or A/D · touch buttons | ✅ Offline |
| 17 | Paddle Duel | sports | P1: W / S · P2: Up / Down · SPACE starts · first to 5 | ✅ Offline |
| 18 | Brick Dash | classic | Left/Right arrows move · SPACE launch/dash · touch buttons | ✅ Offline |
| 19 | Tile Merge | classic | Arrows / WASD · SPACE select · tap tiles + buttons | ✅ Offline |
| 20 | Match Flip | classic | Arrows / WASD move · SPACE flip · tap cards | ✅ Offline |
| 23 | Eaglercraft | action | Minecraft 1.8 defaults: WASD move · mouse look · LMB mine · RMB place · Space jump · E inventory · Q drop · F5 camera · Esc menu | ✅ Offline singleplayer — GPL-3.0 components; **requires owning Minecraft Java Edition** (see `README.md`) |
| 24 | FPS | action | Mouse look · WASD/Arrows move · LMB fire · RMB alt fire · 1/2/3 weapons · R reload · Shift walk · Space hop · Esc pause | ✅ Offline (original raycast renderer) |
| 31 | Letter Boxed | puzzle | Tap letters · A–Z keyboard · Backspace · Enter submit · Esc clears | ✅ Offline |
| 35 | Boss Rush | action | WASD/Arrows move · mouse click or SPACE shoot · R restart · touch FIRE | ✅ Offline |
| 36 | Grid Heist | strategy | 2/3/4 players · Arrows/WASD move · Space claim · Enter start · 4s per turn | ✅ Offline |
| 37 | Last Lantern | action | P1: WASD + Q dash · P2: Arrows + / · P3: IJKL + U · P4: Numpad 8456 + 0 · R restart · Esc menu | ✅ Offline |
| 42 | Queue Escape | strategy | Co-op 1–4: P1 WASD + Space · P2 Arrows + Enter · P3 IJKL + O · P4 TFGH + Y · R restart · Esc menu | ✅ Offline |
| 44 | Story Adventure | puzzle | Tap a choice or press its number key · R restart · M menu (text adventure, 5 endings) | ✅ Offline |
| 47 | Gladihoppers | action | P1/P2 pick WASD, Arrows, or gamepad in the pre-fight menu · mouse/touch for menus | ✅ Offline |
| 48 | Burrito Bison | action | In-game tutorial; mouse/touch to launch and steer the burrito | ✅ Offline |
| 49 | BitLife | simulation | Mouse/touch — pick life choices from menus as your character ages one year per turn | ✅ Offline — compiled IAP/ad/cloud-transfer URLs answered by a local `json/null.json` |
| 50 | Subway Surfers | action | Arrow keys steer/jump/roll · swipe/drag on touch (in-game tutorial prompts reference both) | ✅ Offline — local Unity WebGL build; Kiloo/SYBO backend URLs and the Poki site-lock redirect are answered/blocked locally, so online features are inert |
| 51 | A Dark Room | story | Click/tap the action buttons · keyboard navigates menus | ✅ Offline — original text adventure; Google CDN jQuery + gtag analytics removed |
| 52 | Stranded In Isekai | story | Arrow keys move · SPACE interact/talk/confirm · on-screen D-pad + A button on touch devices | ✅ Offline — original Indonesian-language quiz RPG; fully self-contained |
| 53 | Papa's Pizzeria | strategy | Mouse — click/drag order tickets, toppings, stations and customers (Ruffle Flash emulator; touch via Ruffle pointer events) | ✅ Offline — local SWF + bundled local Ruffle; MochiAds/FliplineAds ad calls answered locally |
| 54 | Retro Bowl | sports | Mouse/touch — tap menu buttons, tap/swipe to pass, throw, tackle and kick | ✅ Offline — local GameMaker:HTML5 build; Poki SDK replaced with a local no-op stub (no poki.com sitelock); store/social links are user-initiated `window.open` only; a SupersonicAds injector function exists but is never invoked |
| 55 | Super Hot | action | Mouse look · WASD move · click shoot · touch to move/look | ✅ Offline — local Unity WebGL build; site scripts (Google Analytics, DMCA badge, tab cloak) removed |
| 56 | 10 Minutes Till Dawn | action | WASD/Arrows move · mouse aim · click/LMB shoot · choose upgrades on level-up | ✅ Offline — local Unity WebGL build; Google Analytics and tab cloak removed |
| 57 | Fleeing the Complex | story | Mouse/tap — click choices at decision points (Flash via Ruffle) | ✅ Offline — local SWF + bundled local Ruffle; unpkg CDN-first Ruffle loader removed |
| 58 | Infiltrating the Airship | story | Mouse/tap — click choices at decision points (Flash via Ruffle) | ✅ Offline — local SWF + bundled local Ruffle; unpkg CDN-first Ruffle loader removed |
| 59 | Baldi's Basics | action | WASD/Arrows move · mouse look · Shift run · click or E interact · Esc pause | ✅ Offline — local Unity WebGL build (Mystman12, v1.4.3); gtag, cloak, absolute `/js/main.js`, remote favicon and unused Firebase SDK removed |
| 60 | Temple Run 2 | action | Swipe/drag to turn, jump and slide · arrow keys on desktop | ✅ Offline — local Babylon.js port; gtag, cloak, Poki SDK (loader + core ad SDK) removed, local no-op Poki stub, XHR/fetch hook answers Poki/Babylon CDN/typekit hosts |

---

## Notes on specific games

- **2048 (1):** the local build binds only `W/A/S/D` — the arrow keys and touch/swipe are not wired up in this copy. The external reference is a favicon only; gameplay is local.
- **Ovo (7):** the original bundle contains an ad-block-detection XHR (`api.adinplay.com`) and a tips fetch (`dedragames.com/games/ovo/.../tips.json`). Offline these fail silently and the game still plays from the local `data.js`/assets.
- **Run 3 (8):** gameplay is fully local; the bundle carries external link hooks (an ad-click URL and store/credits links) that open in new tabs — they are not game-state fetches.
- **Age of War (2):** the game is a Flash `.swf` played through Ruffle, which is loaded from `https://unpkg.com/@ruffle-rs/ruffle`. Without network, Ruffle never loads and the game cannot start.
- **Gladihoppers (47):** vendored Unity WebGL build (v3.0.1, Dreamon Studios) from `github.com/1000unblockedgames/Gladihoppers` (commit `a14cd76`); ad/analytics scripts removed and a local no-op Poki SDK stub keeps it fully offline — the online-PvP and IAP buttons are inert without network.
- **Burrito Bison (48):** fan-hosted HTML5 port (Unity WebGL) of Juicy Beast Studio's *Burrito Bison*, vendored from `github.com/a456pur/seraph` (branch `main`, path `games/burritobison`); Google Analytics removed. The port's Kongregate SDK (`Build/kongregate_api.js`) is **retained** so the game's API glue still resolves, but its network features are inert offline (the Swrve analytics batch POST needs a Kongregate-hosted config that never arrives, and the only external domain it would touch, `api.swrve.com`, is guarded). The port's `UnityUrlFix` hook routes any `unity3d.com`/`appspot.com`/`swrve.com`/`kongregate.com`/`cloudmoolah.com`/`script.google.com`/`googleusercontent.com` request (Unity IAP/analytics SDK calls, Kongregate SDK network endpoints, and the port's cloud-save/data backends) to a local `json/null.json`, so those in-game store/analytics/cloud features are inert/guarded offline.
- **BitLife (49):** fan-hosted HTML5 port (Unity WebGL, `companyName` "3kh0.github.io") of Candywriter, LLC's *BitLife* life simulator, vendored from `github.com/a456pur/seraph` (branch `main`, path `games/bitlife`); Google Analytics and the site's tab-cloak script removed. The compiled port embeds Unity IAP (`*.iap.unity3d.com`), Candywriter ad-config (`cywr*.appspot.com`), and cloud-transfer (`unitygame.herokuapp.com/bitlife/transfer_config.txt`) URLs — an XHR hook routes any request to those hosts (plus `amongus-online.net`) to a local `json/null.json`, so the in-game store, ads, and cloud-transfer features are inert offline. Social/website/store links remain user-initiated browser links only.
- **Subway Surfers (50):** fan-hosted HTML5 port (Unity WebGL build `SanFrancisco`, v1.94.0, `companyName` "Kiloo Games") of Kiloo and SYBO Games' *Subway Surfers*, vendored from `github.com/a456pur/seraph` (branch `main`, commit `ae2fcc6`, path `games/subwaysurfers`). Google Analytics (gtag), the 4399 ad SDK, the Poki SDK (loader + core), and the tab-cloak script are removed; a local no-op Poki stub answers the compiled `_JS_PokiSDK_*` glue calls (including the "ready" bridge handshake), and the loader's `_JS_Eval_OpenURL` is patched to skip the `poki.com/sitelock` redirect (the 4399 loader this build shipped with had no such function, so skipping it matches the upstream behavior). The compiled port embeds Kiloo/SYBO backend URLs (daily quests, auth, cloud saves, online settings, purchase validation, analytics, `subway.azureedge.net`) plus Facebook/Google Sheets logging — an XHR/fetch hook routes any request to those hosts to a local `json/null.json`, so online services are inert offline. Social/website/store links remain user-initiated browser links only.
- **A Dark Room (51):** the original minimalist text adventure by **Doublespeak Games** (Michael Townsend), vendored from the source/fan-hosted repo `github.com/doublespeakgames/adarkroom` under the **Mozilla Public License 2.0** (`LICENSE.md` included). Google CDN jQuery and Google Analytics (gtag) are removed — jQuery loads from the local `lib/` copy, audio loads from the local `audio/` folder, and all 25 language packs are vendored. The menu's GitHub/app-store/share links are user-initiated browser links only.
- **Papa's Pizzeria (53):** the original 2007 Flash time-management game by **Flipline Studios**, vendored from the fan-hosted archive `github.com/a456pur/seraph` (branch `main`, path `games/papaspizzaria`) as a self-contained folder: the game SWF (`papaspizzaria.swf`) plus the archive's bundled Ruffle emulator (`ruffle.js` + `c5c02c4e65c1c4423a97.wasm`), which runs the SWF without the Flash plugin. Google Analytics (gtag), the site's tab-cloak script, the absolute `/js/main.js` reference, and the unpkg CDN-first Ruffle loader were removed — Ruffle now loads from the local copy only. The SWF embeds ad-SDK hosts (MochiAds `x.mochiads.com`, FliplineAds `fliplineads.com`, and Chinese portal SDKs `3366.com`/`3366img.com`/`4399.com`/`7k7k.com`/`freezingpointe.com`/`2144.cn`) — an XHR/fetch hook routes any request to those hosts to a local `json/null.json`, so ads are inert offline. The game's store/site promo buttons (iTunes, Google Play, Amazon, flipline.com, papalouie.com) are user-initiated browser links only. Progress saves via Flash SharedObject, stored locally by Ruffle. The SWF is the property of Flipline Studios and is included for fan/archival play.
- **Retro Bowl (54):** fan-hosted HTML5 port (GameMaker:HTML5 build) of New Star Games' *Retro Bowl*, sourced via `github.com/deploythings123123123/seraph` (branch `main`, commit `ae2fcc6`, path `games/retrobowl`); the port's `README.md` and `LICENSE` are retained. Google Analytics (gtag) and the site's tab-cloak script are removed; the Poki SDK init + `poki.com/sitelock` redirect wrapper is replaced with a local no-op `window.PokiSDK` stub, and the port's Poki glue (`html5game/uph_poki.js`) is retained so the game's API calls resolve inertly offline. The compiled bundle contains a SupersonicAds `delivery.min.gz.js` injector function that is **defined but never invoked** (dead code), plus user-initiated store/social `window.open` links (App Store, Google Play, newstargames.com, Twitter, Reddit) and third-party library license comments (jsgif, zlib.js, murmurhash, Valve fingerprintjs) — none are gameplay fetches.
- **Super Hot (55):** fan-hosted HTML5 port (Unity WebGL build) of the SUPERHOT franchise, sourced via `github.com/deploythings123123123/seraph` (branch `main`, commit `ae2fcc6`, path `games/superhot`). Google Analytics (gtag), the site's tab-cloak script, and the script-injection routines (second GA tag + DMCA badge image/helper) are removed from `index.html`/`main.js`; the author credit line in `main.js` (echo-the-coder) is retained. The compiled `webgl.jsgz` contains no external URLs; the `.data`/`.mem` blobs contain only inert font-license/credit strings (SIL OFL, Ascender Corp, iTween, .NET schema URIs). Unreferenced orphan file `97c35fd0-ea33-4844-a283-b7a4c6942643` was not vendored.
- **10 Minutes Till Dawn (56):** fan-hosted HTML5 port (Unity WebGL build, `companyName` "Flanne") of Flanne's *10 Minutes Till Dawn*, sourced via `github.com/deploythings123123123/seraph` (branch `main`, commit `ae2fcc6`, path `games/10minutestilldawn`). Google Analytics (gtag) and the site's tab-cloak script are removed; the remote `../../images/ico.ico` favicon is replaced with the local `icon.png`. The compiled wasm/data/framework files contain only inert strings (Unity/MS docs links, SIL OFL + Ascender Corp font credits, and a Steam store link string in the data blob) — no runtime fetches.
- **Fleeing the Complex (57):** fan-hosted port (original Flash SWF) of PuffballsUnited's Henry Stickmin game *Fleeing the Complex*, sourced via `github.com/deploythings123123123/seraph` (branch `main`, commit `ae2fcc6`, path `games/fleeingthecomplex`). Google Analytics (gtag), the site's tab-cloak script, the absolute `/js/main.js` reference, and the unpkg CDN-first Ruffle loader are removed — Ruffle now loads from the bundled local copy (`ruffle/`, Apache-2.0/MIT licensed per the vendored `LICENSE_APACHE`/`LICENSE_MIT`). The SWF contains no external URL strings; it is the property of PuffballsUnited and is included for fan/archival play.
- **Infiltrating the Airship (58):** fan-hosted port (original Flash SWF) of PuffballsUnited's Henry Stickmin game *Infiltrating the Airship*, sourced via `github.com/deploythings123123123/seraph` (branch `main`, commit `ae2fcc6`, path `games/infiltratingtheairship`). Google Analytics (gtag), the site's tab-cloak script, the absolute `/js/main.js` reference, and the unpkg CDN-first Ruffle loader are removed — Ruffle now loads from the bundled local copy (`ruffle/`, Apache-2.0/MIT licensed per the vendored `LICENSE_APACHE`/`LICENSE_MIT`). The SWF contains no external URL strings; it is the property of PuffballsUnited and is included for fan/archival play.
- **Baldi's Basics (59):** fan-hosted Unity WebGL build (v1.4.3, `companyName` "Mystman12") of Mystman12's *Baldi's Basics in Education and Learning*, sourced via `github.com/deploythings123123123/seraph` (branch `main`, commit `ae2fcc6`, path `games/baldisbasics`). Google Analytics (gtag), the site's tab-cloak script, the absolute `/js/main.js` reference, the remote `../../images/ico.ico` favicon (replaced with the local `splash.png`), and the unused Firebase SDK (`firebase-app.js` — the compiled build never touches `window.firebase`) are removed. The compiled wasm/data blobs contain only inert strings (font licenses, .NET schema URIs); there are no runtime fetches. The game is the property of Mystman12 and is included for fan/archival play.
- **Temple Run 2 (60):** fan-hosted HTML5 port (Babylon.js, webpack bundle `temple_run_2_web`) of the *Temple Run 2* endless-runner concept, sourced via `github.com/deploythings123123123/seraph` (branch `main`, commit `ae2fcc6`, path `games/templerun2`). Google Analytics (gtag), the site's tab-cloak script, the remote `../../images/ico.ico` favicon, and the Poki SDK (loader + core, an ad SDK that talks to poki.io/ad servers) are removed; a local no-op `poki-noop.js` stub answers the compiled `window.PokiSDK` glue (init resolves so the game boots; commercialBreak/rewardedBreak resolve so continue/revive flows close), and an XHR/fetch hook routes any request to `poki.com`, Babylon.js CDN hosts (`assets/preview/controllers/snippet/doc/playground.babylonjs.com`), `unpkg.com` or `use.typekit.net` to a local `json/null.json`. The bundle's remaining external URL strings (reactjs.org warning links, `assets.babylonjs.com` environment defaults, the typekit link used only by popup-window helpers) are inert: the game loads its skybox (`Machu_Skybox.glb`), textures, OGG audio and the draco decoder from the local `assets/` folder, and webpack chunks (e.g. `772.bundle_original.js`) load via the automatic publicPath, which is relative to the game folder.
- **Stranded In Isekai (52):** an original HTML5/JS RPG (Indonesian-language content) vendored as-is. The bundled `README.md` and the `flashRPG.js` source header credit the author as **Daffa Ahmad Ibrahim** and request credit when reused; an **Apache-2.0** `LICENSE` file is included (the author's email link/string was removed, attribution retained). Fully self-contained (local images/sounds, no network calls); the engine is keyboard-driven (arrows + SPACE, no mouse handling), and a mobile on-screen control overlay (D-pad + A button) was added in `index.html` that dispatches the same key events.
- **Random sports games (4, 5, 6):** Construct 3 exports with local assets (`box2d.wasm.js`, `scripts/offlineclient.js`, `scripts/main.js`). Leftover `ubg235`/analytics files exist in the folders but are **not loaded** by `index.html`, so they don't affect offline play.
- **Eaglercraft (23):** relays and server list are empty by default (`relays: null`, `servers: null` in `index.html`). Clicking **Singleplayer** creates a local offline world. Licensing: GPL-3.0 components + MC 1.8 assets require owning Minecraft Java Edition.
- **Catalog pruning (2026-08):** 18 self-built games were removed from `Games/` and the catalog: A-GEO Quiz (21), Connections (22), Memory Match (25), Multiplayer Arena Shooter (26), Sudoku (27), Times Tables (28), Typing Test (29), Word Scramble (30), Mental Math (32), Spelling Bee (33), Balance Beam Bash (34), Orbit Relay (38), Parcel Panic (39), Pattern Panic (40), Pocket Bumper (41), Signal Sprint (43), Word Relay Riot (45), Wordle (46). Their `games.json` ids were removed and are **not reused**; retained entries keep their original ids.

## Unregistered folders

- `Games/QWOP/` — a standalone QWOP-style game folder that is **not** referenced by `games.json`. The registered "QWOP" entry (id 15) points to `Games/QwopRemake/index.html`. Left untouched; flagged here for awareness.

## Provenance quick view

- **Original code (written for this repo):** FPS, Star Catcher, Paddle Duel, Brick Dash, Tile Merge, Match Flip, Letter Boxed, Boss Rush, Grid Heist, Last Lantern, Queue Escape, Story Adventure. (The 18 self-built games removed in the 2026-08 pruning were also original code; see the pruning note above.)
- **Third-party bundles (attribution/licensing kept):** Ovo, Run 3, Snake, Chrome Dino, Breakout, QWOP remake, Soccer/Basket/Volley Random (Construct 3 exports), Eaglercraft (GPL-3.0 + MC assets), Gladihoppers (Unity WebGL, Dreamon Studios), Burrito Bison (fan-hosted Unity WebGL port of Juicy Beast Studio's game, via `github.com/a456pur/seraph`), BitLife (fan-hosted Unity WebGL port of Candywriter, LLC's game, via `github.com/a456pur/seraph`), Subway Surfers (fan-hosted Unity WebGL port of Kiloo + SYBO Games' game, via `github.com/a456pur/seraph`), A Dark Room (Doublespeak Games, MPL-2.0, via `github.com/doublespeakgames/adarkroom`), Stranded In Isekai (original HTML5/JS RPG by Daffa Ahmad Ibrahim per the bundled README/flashRPG.js headers, Apache-2.0), Papa's Pizzeria (original Flash game by Flipline Studios, SWF + bundled Ruffle emulator, via `github.com/a456pur/seraph`), Retro Bowl (fan-hosted GameMaker:HTML5 port of New Star Games' game, via `github.com/deploythings123123123/seraph`), Super Hot (fan-hosted Unity WebGL port of the SUPERHOT franchise, via `github.com/deploythings123123123/seraph`), 10 Minutes Till Dawn (fan-hosted Unity WebGL port of Flanne's game, via `github.com/deploythings123123123/seraph`), Fleeing the Complex (original Flash game by PuffballsUnited, SWF + bundled Ruffle emulator, via `github.com/deploythings123123123/seraph`), Infiltrating the Airship (original Flash game by PuffballsUnited, SWF + bundled Ruffle emulator, via `github.com/deploythings123123123/seraph`), Baldi's Basics (fan-hosted Unity WebGL port of Mystman12's game, via `github.com/deploythings123123123/seraph`), Temple Run 2 (fan-hosted Babylon.js port of the Temple Run 2 endless-runner concept, via `github.com/deploythings123123123/seraph`).
- **Read-only:** `Games/Character AI/`.
